# Memory - 2026-02-13

## OpenClaw Channel Configuration Discussion

User asked about configuring channels for mobile communication with OpenClaw agent.

### Key Points:

1. **Current Configuration**: Channels array is empty (`{}`) in `openclaw.json`
   - Only webchat is currently available on port 18789

2. **Available Channel Plugins** (installed but not configured):
   - **QQ Bot** - QQ official bot API (simplest setup)
   - **DingTalk** - Enterprise internal bot with Stream mode (no public IP needed)
   - **Enterprise WeChat** - Requires enterprise certification

3. **QQ Bot Configuration**:
   - Requires: AppID and ClientSecret from QQ Open Platform
   - Can be configured via `openclaw onboard qqbot` command
   - Supports: Direct messages, group messages, images
   - Documentation: https://q.qq.com/wiki/

4. **DingTalk Configuration**:
   - Requires: ClientID, ClientSecret, RobotCode from DingTalk Developer Console
   - Uses Stream mode (WebSocket) - no public IP required
   - Supports: Text, Markdown, AI Interactive Cards, images, voice, video
   - Can use `messageType: "markdown"` or `"card"` (streaming updates)
   - Documentation: `~/.openclaw/extensions/dingtalk/README.md`

5. **Enterprise WeChat**:
   - Not currently installed in plugins
   - Requires enterprise certification

### Decision:
User is evaluating which channel to configure based on their needs and available credentials.